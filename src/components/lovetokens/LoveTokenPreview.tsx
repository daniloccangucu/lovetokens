import { LoveToken } from "../../models/LoveToken"

function LoveTokenPreview(loveTokens: LoveToken[]) {
    return (
        <div>
            Love Token
        </div>
    )
}

export default LoveTokenPreview
